- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: SpaceVim
    ~/.SpaceVim.d: SpaceVim.d
    ~/.zshrc: sites/mac/zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
